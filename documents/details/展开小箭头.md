# 完整修改方案
## 一、先改UI：让树节点左边显示「展开小箭头」（你说的小标签）
你现在树节点左边没有展开折叠的小三角，是因为属性没开，10秒搞定：
1.  打开 `mainwindow.ui`，选中左侧的 `treeWidgetAlbum` 树控件
2.  右侧属性栏，找到这3个属性，全部打勾：
    - ✅ `rootIsDecorated`：**核心**，控制根节点左边显示展开/折叠箭头
    - ✅ `itemsExpandable`：允许节点展开折叠
    - ✅ `headerHidden`：隐藏多余的表头，界面更干净
3.  保存UI文件，箭头就会正常显示了。

---

## 二、修改代码：创建项目后自动进入默认相册
### 核心修改点：
1.  修复你代码里的**字段名大小写不匹配**的潜在bug（否则拿不到路径）
2.  创建项目/加载项目后，自动选中「默认相册」节点
3.  选中后自动触发刷新，右侧直接显示相册内容，不用手动点击

### 1. 先修复向导字段注册（必须改，否则拿不到用户输入）
打开你的 `ProSetPage.cpp`，找到注册字段的代码，改成和你MainWindow里一致的名字：
```cpp
// 必须和你MainWindow里的field名完全一致，大小写敏感！
registerField("projectName", ui->lineEdit_name);
registerField("projectpath", ui->lineEdit_path); // 你MainWindow里写的是projectpath，这里必须对应
```

### 2. 修改MainWindow.cpp的核心代码
#### ① 修复「创建项目」槽函数（匹配你的代码风格）
```cpp
//点击"创建项目按钮"
void MainWindow::on_actioncreate_triggered()
{
    Albumwizard wizard(this);
    if(wizard.exec() == QDialog::Accepted){
        //从向导获得用户输入（和向导里registerField的名字完全一致）
        QString name = wizard.field("projectName").toString();
        QString path = wizard.field("projectpath").toString();
        //调用数据层创建项目
        if(AlbumProject::getInstance().createProject(name, path)){
            loadProject(); // 加载项目后，会自动选中默认相册
        }
    }
}
```

#### ② 修改 `initTreeWidget` 函数（自动展开根节点）
```cpp
// 初始化目录树
void MainWindow::initTreeWidget()
{
    // 根节点：项目名
    QTreeWidgetItem* rootItem = new QTreeWidgetItem(ui->treeWidgetAlbum);
    rootItem->setText(0, AlbumProject::getInstance().getProjectName());
    rootItem->setData(0, Qt::UserRole, "root"); // 标记为根节点

    // 遍历所有相册，创建子节点
    refreshTreeWidget();

    // 默认展开根节点（展开箭头自动变成展开状态）
    ui->treeWidgetAlbum->expandItem(rootItem);
}
```

#### ③ 修改 `refreshTreeWidget` 函数（核心：自动选中默认相册）
```cpp
// 刷新目录树：只更新相册子节点，不重建根节点
void MainWindow::refreshTreeWidget()
{
    // 获取根节点
    QTreeWidgetItem* rootItem = ui->treeWidgetAlbum->topLevelItem(0);
    if(!rootItem) return;

    // 清空所有相册子节点
    rootItem->takeChildren();

    // 从数据层获取最新相册列表，重新创建节点
    QStringList albumList = AlbumProject::getInstance().getAlbumList();
    QTreeWidgetItem* firstAlbumItem = nullptr; // 记录第一个相册（默认相册）

    for(const QString& albumName : albumList) {
        QTreeWidgetItem* albumItem = new QTreeWidgetItem(rootItem);
        albumItem->setText(0, albumName);
        albumItem->setData(0, Qt::UserRole, "album"); // 标记为相册节点
        albumItem->setData(0, Qt::UserRole+1, albumName); // 存储相册名

        // 记录第一个相册节点（就是默认相册）
        if(!firstAlbumItem) {
            firstAlbumItem = albumItem;
        }
    }

    // 核心：自动选中第一个相册（默认相册），触发右侧缩略图刷新
    if(firstAlbumItem) {
        ui->treeWidgetAlbum->setCurrentItem(firstAlbumItem);
        // 手动触发选中事件，直接进入相册
        onTreeItemSelected(firstAlbumItem, 0);
    }
}
```

#### ④ 补充：确保你的选中事件函数和下面一致（不用改，只是确认）
```cpp
// 目录树选中事件处理
void MainWindow::onTreeItemSelected(QTreeWidgetItem* item, int column)
{
    Q_UNUSED(column);
    if(!item) return;

    QString nodeType = item->data(0, Qt::UserRole).toString();
    if(nodeType == "album") {
        // 选中相册节点：刷新右侧缩略图
        QString albumName = item->data(0, Qt::UserRole+1).toString();
        refreshPhotoList(albumName);
    } else {
        // 选中根节点：清空右侧缩略图
        ui->listWidgetPhoto->clear();
    }
}
```

---

## 三、修改后效果&测试验证
1.  ✅ 左侧树节点左边**正常显示展开/折叠的小箭头**（你说的小标签）
2.  ✅ 点击创建项目，完成向导后，自动选中「默认相册」节点
3.  ✅ 右侧自动刷新，直接进入默认相册，不用手动点击
4.  ✅ 根节点默认展开，箭头自动变成展开状态
5.  ✅ 打开已有项目时，也会自动选中第一个相册，直接进入

---

## 关键说明
1.  **字段名大小写必须一致**：Qt的`field()`是大小写敏感的，向导里`registerField`的名字，必须和MainWindow里`wizard.field()`的名字完全一样，否则会拿不到空字符串，创建项目失败。
2.  **架构完全延续**：所有数据逻辑还是在`AlbumProject`里，MainWindow只负责UI调度，没有破坏之前的分层架构。
3.  **兼容后续功能**：后续你加双击看大图、导入图片等功能，完全不受影响，直接在这个基础上写就行。
